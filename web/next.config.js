/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'http://localhost:3333',
      'http://localhost:3333',
      'localhost',
    ],
  },
}

module.exports = nextConfig
